<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash + activity theme.  We use Material3 with no decor color so
         our Compose theme owns rendering once the activity inflates.  The
         "Light.NoActionBar.Bridge" style keeps the activity skeletal —
         Compose handles status-bar coloring via WindowCompat. -->
    <style name="Theme.Runtime" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
