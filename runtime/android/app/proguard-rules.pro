# Add project-specific ProGuard rules here.
# Compose/Kotlin/AndroidX defaults already cover the standard cases via
# their own consumer-rules.  Add module-specific keep rules below as
# native modules and reflective consumers land.
