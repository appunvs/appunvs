<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only ATS opening: allow cleartext HTTP to the emulator's host
  bridge (10.0.2.2) and to localhost so the Android emulator can reach
  a relay running on the developer's machine.  Production builds tighten
  this to https-only via a release-flavor override later.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
