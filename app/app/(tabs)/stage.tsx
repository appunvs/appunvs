import { StagePanel } from '@/screens/StagePanel';

// Stage tab — full-bleed, focus mode.  Useful for demos / debugging
// even on wide screens, even though the Chat tab also embeds a Stage
// panel side-by-side.
export default function StageTab() {
  return <StagePanel />;
}
